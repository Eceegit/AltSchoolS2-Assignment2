import React from 'react'

const NavigationPage = ({isActive}) => {

  return {
    // color: isActive ? 'red' : 'purple', 
    // fontWeight: isActive ? 'bold' : 'normal',
    // textDecoration: isActive ? 'none' : "underline"
  }
}

export default NavigationPage